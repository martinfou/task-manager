# Scripts (Google Tasks app)

| Script | Purpose |
|--------|---------|
| [`backup-google-tasks.sh`](backup-google-tasks.sh) | Server backup: DB dump + `storage/` (no logs), tiered retention, optional **rclone** to Dropbox |

**Documentation**: [docs/BACKUP_RESTORE.md](../docs/BACKUP_RESTORE.md) (schedule, retention, restore runbook, security).
